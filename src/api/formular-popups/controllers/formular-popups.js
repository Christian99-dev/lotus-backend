'use strict';

/**
 * formular-popups controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::formular-popups.formular-popups');
